# Name of your application. Used to uniquely configure containers.
service: mikereader

# Name of the container image.
image: mmcrockett/mikereader

# Deploy to these servers.
# NOTE: temporarily pointed at the existing Debian 11 box (dreamcompute) for
# early validation while the new Debian 12 box is still being provisioned --
# see 07-docker-kamal-deploy.md. This box already serves production traffic
# for the old app via nginx+Certbot on reader.mmcrockett.com -> 127.0.0.1:3100,
# so kamal-proxy is rebound to a different port below (3200) rather than the
# default 80/443, to avoid colliding with nginx or the old app's port.
servers:
  web:
    - 208.113.128.139
  # job:
  #   hosts:
  #     - 208.113.128.139
  #   cmd: bin/jobs

# nginx on this box already owns 80/443 (Certbot-managed for reader.mmcrockett.com,
# proxying to the old app on 3100). kamal-proxy is rebound to 3200 here so it
# doesn't collide -- see `builder`/`proxy.run` below. No host/ssl set: this is
# for direct IP:port validation, not routed through nginx/DNS yet.
proxy:
  run:
    http_port: 3200

# Where you keep your container images.
registry:
  server: ghcr.io
  username: mmcrockett

  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
  clear:
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # The app's primary database lives on the existing production MySQL host,
    # not a Kamal-managed accessory -- see config/database.yml. No DB_HOST
    # override needed here.

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files (Solid Queue/Cache/Cable)
# and local Active Storage files. Recommended to change this to a mounted volume
# path that is backed up off server.
volumes:
  - "mikereader_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server

# This box's SSH access is via a non-root "debian" user + dedicated key
# (matches the local ssh-dh-compute shell alias).
ssh:
  user: debian
  keys: [ "~/.ssh/dreamcomputeserverpw" ]

# Use accessory services (secrets come from .kamal/secrets).
# No accessories needed: MySQL is the existing production host (external,
# not managed by Kamal), and Redis was dropped in favor of Solid Cable.
